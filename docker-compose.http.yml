# ============================================
# Docker Compose Override - HTTP Mode (MITM Demo)
# Use: docker-compose -f docker-compose.yml -f docker-compose.http.yml up
# ============================================

services:
  nginx:
    volumes:
      - ./nginx/nginx.http.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
